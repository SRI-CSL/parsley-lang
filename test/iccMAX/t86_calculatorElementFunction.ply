// Table 86 of iccMAX documentation, page 101 of the specification

type function_operations = {
    // add the operations from the spec
}

type cefe_type = {
    // number of operations, N
    n: int,
    // todo: function operations
    f: function_operations
}

format {
    CalculatorElementFunctionEncoding cefe {c: cefe_type} :=
        !"func"!
        !"\x00\x00\x00\x00"!
        n = UInt32<endian=endian::Big()>

        {
            cefe.c.n := n;
        };;
}
