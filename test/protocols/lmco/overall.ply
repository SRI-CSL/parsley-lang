use {heartbeat, sys_status}
format {
CRC crc {crc: int} := 
    uint16=(Byte ^ 2)
    {   
        crc.crc := convert(List.rev(uint16))
    }
;;

MavlinkPacket mp {
        packet: [byte],
        mp_crc: int
      } := 
    (hb=Heartbeat | ss=SysStatus)
}
